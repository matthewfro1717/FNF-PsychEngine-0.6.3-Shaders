function onUpdate(elapsed)
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singLEFT') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singDOWN') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singUP') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singRIGHT') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singLEFT-alt') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singDOWN-alt') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singUP-alt') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'singRIGHT-alt') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'PhoneSmash') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'PhoneThrow') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
    if stringStartsWith(getProperty'dad.animation.curAnim.name', 'scream') and getProperty 'dad.animation.curAnim.finished' then
        runHaxeCode 'game.dad.dance();'
    end
end
